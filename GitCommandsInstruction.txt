git clone <repo_url> -- clone a repo locally
git pull -- To get online changes locally.


1.git add -A -- Add all files to git
2. git commit -m "MSG" : Make a commit Locally.
3. git push -u origin master 

git status : To see Status of Files.
